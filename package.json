{
  "name": "cardsight",
  "private": true,
  "scripts": {
    "dev": "pnpm --parallel --filter client --filter server dev",
    "dev:server": "pnpm --filter server dev",
    "dev:client": "pnpm --filter client dev",
    "build": "pnpm --filter shared build && pnpm --filter server prisma generate && pnpm --parallel --filter client --filter server build",
    "db:migrate": "pnpm --filter server prisma migrate dev",
    "db:seed": "pnpm --filter server prisma db seed",
    "db:seed-date-night": "pnpm --filter server exec node --import tsx/esm prisma/seed-date-night.ts",
    "db:seed-date-night:prod": "pnpm --filter server db:seed-date-night:prod",
    "db:seed-would-you-rather": "pnpm --filter server exec node --import tsx/esm prisma/seed-would-you-rather.ts",
    "db:seed-would-you-rather:prod": "pnpm --filter server db:seed-would-you-rather:prod",
    "db:seed-test": "pnpm --filter server exec tsx prisma/seed-test-game.ts",
    "db:seed-qrians": "pnpm --filter server exec tsx prisma/seed-qrians.ts",
    "db:studio": "pnpm --filter server prisma studio"
  },
  "volta": {
    "node": "24.11.0",
    "pnpm": "10.18.3"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@prisma/client",
      "@prisma/engines",
      "esbuild",
      "prisma"
    ]
  },
  "devDependencies": {
    "puppeteer": "^24.40.0"
  }
}
